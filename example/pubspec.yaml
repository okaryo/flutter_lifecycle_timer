name: flutter_lifecycle_timer_example
description: A new Flutter project.

publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_lifecycle_timer:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
